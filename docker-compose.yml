services:
    app:
        build:
            context: .
            dockerfile: Dockerfile.dev
        container_name: nuxt-enterprise-template-dev
        ports:
            - '${PORT:-3000}:${PORT:-3000}'
        env_file:
            - .env
        volumes:
            - ./:/app
            - /app/node_modules
